<br>
<h1>My Tasks</h1>
<ul>
<% @tasks.each do |task| %>
  <li><%= task.completed ? "completed" : "not completed" %> - <%= link_to task[:title], task_path(task)%> - <%= link_to "🗑", destroy_task_path(task), method: :delete, data: { confirm: "Are you sure?" }%></li>
<% end %>
</ul>

<%= link_to "Add a new task?", new_task_path %>
